import 'package:flutter/material.dart';

class ThirdOnboarding extends StatelessWidget {
  const ThirdOnboarding({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Third Onboarding'),
    );
  }
}
